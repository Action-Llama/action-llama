---
"@action-llama/action-llama": patch
---

Remove `al push` SSH deploy command and all associated VPS deployment infrastructure (`src/remote/`, `src/shared/server.ts`, `al env init`, `al env logs`).
