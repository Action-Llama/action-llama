---
"@action-llama/action-llama": patch
---

Add `-a`/`--all` flag to `al logs` to show all log levels without filtering. By default, conversation mode hides debug entries and internal messages; `--all` disables this filtering while keeping the readable conversation-style formatting.
