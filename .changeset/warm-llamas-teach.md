---
"@action-llama/action-llama": patch
---

Add `setenv` documentation to the agent commands reference page as a new "Environment Commands" section, with usage examples showing cross-invocation variable persistence. Update runtime context docs to cross-reference the new section.
