---
"@action-llama/action-llama": patch
---

Surface agent runtime errors in the dashboard so container stderr and failed runs show the latest error message in the web UI instead of only in backend logs.
