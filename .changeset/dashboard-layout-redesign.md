---
"@action-llama/action-llama": patch
---

Redesign dashboard layout: recent triggers now display full-width above the agents table (showing last 5 with a new Source column), agents table gains a Description column, searchable filtering, and CSS-based name truncation instead of hard character limits.
