---
"@action-llama/action-llama": patch
---

Show decoded body on webhook receipt page when the payload is form-urlencoded or base64-encoded. The decoded view appears above the raw body, with JSON values within form params automatically parsed.
