---
"@action-llama/action-llama": patch
---

Show decoded body on webhook receipt and trigger detail pages when the payload is form-urlencoded or base64-encoded. The decoded view appears above the raw body, with JSON values within form params automatically parsed.
