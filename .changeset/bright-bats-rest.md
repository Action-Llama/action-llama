---
"@action-llama/action-llama": patch
---

When shared base images are building during startup, agents now report a `building` status in the TUI and dashboard instead of appearing idle. Closes #585.
