---
"@action-llama/action-llama": patch
---

Add summarize modal with custom prompt support. Clicking "Summarize" on activity rows or instance logs now opens a modal where you can edit the prompt sent to the model. Also fixes log summary searching only the latest daily log file (now searches all files) and resolves API key auth for pi_auth models.
