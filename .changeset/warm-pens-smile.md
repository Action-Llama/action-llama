---
"@action-llama/action-llama": patch
---

Show full error details in `al logs` for container entry failures. Previously only the message "container entry error" was displayed with no detail; now the error message, stack trace, and any extra fields are shown.
