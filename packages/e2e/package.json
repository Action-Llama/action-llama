{
  "name": "@action-llama/e2e",
  "version": "0.1.2",
  "private": true,
  "description": "End-to-end tests for Action Llama",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:coverage": "AL_COVERAGE=1 vitest run",
    "test:local": "docker-compose up --build --abort-on-container-exit"
  },
  "devDependencies": {
    "@action-llama/action-llama": "*",
    "@action-llama/shared": "*",
    "@types/node": "^22.0.0",
    "dockerode": "^4.0.2",
    "playwright": "^1.58.0",
    "ssh2": "^1.15.0",
    "tar-fs": "^3.0.6",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
